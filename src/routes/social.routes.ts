// import { Router } from 'express'

// const router = Router()

// router.get('/profile/:profileId', SocialController.getConnections)
// router.delete('/:connectionId', SocialController.deleteConnection)
// router.put('/:connectionId', SocialController.updateConnection)

// export default router